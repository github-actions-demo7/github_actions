name: First WorkFlow
on: [push]

jobs:
  run-shell:
  runs-on: ubuntu-latest
  steps:
    - name: echo string
      run : echo "Hello world"
    - name : Multi line cmds
      run : |
        node -v
        npm -v